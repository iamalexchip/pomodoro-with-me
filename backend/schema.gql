# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime

type Mutation {
  createColumn(label: String!, session: String!): Session!
  createSession(name: String!, slug: String!): Session!
  createTask(column: String!, session: String!, title: String!): Task!
  deleteColumn(id: String!, session: String!): Session
  deleteSession(slug: String!): Boolean
  deleteTask(id: String!): Boolean
  toggleSession(slug: String!, status: SessionStatus!): Session!
  updateColumn(id: String!, isFocus: Boolean, label: String, position: Float, session: String!): Session!
  updateSession(isModerated: Boolean!, isOpen: Boolean!, name: String!, slug: String!): Session!
  updateTask(column: String, id: String!, position: Float, title: String): Task!
}

type Query {
  getSession(slug: String!): Session
  listSessions: [Session!]!
  listTasks(session: String!): [Task!]!
}

"""The Session model"""
type Session {
  columns: [SessionColumn!]!
  end: DateTime
  id: ID!
  isModerated: Boolean!
  isOpen: Boolean!
  name: String!
  slug: String!
  status: SessionStatus!
  timesheet: [TimeEntry!]!
}

"""The Session column model"""
type SessionColumn {
  id: ID!
  isFocus: Boolean!
  label: String!
  position: Float!
}

enum SessionStatus {
  break
  done
  pomodoro
  unbegun
}

"""The Task model"""
type Task {
  column: String!
  createAt: DateTime!
  id: ID!
  position: Float!
  session: Session
  timesheet: [TimeEntry!]!
  title: String!
  updateAt: DateTime!
}

"""Time entry model"""
type TimeEntry {
  end: DateTime
  id: ID!
  start: DateTime
}
