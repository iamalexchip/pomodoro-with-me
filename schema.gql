# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime

type Mutation {
  createColumn(label: String!, session: String!): Session!
  createSession(name: String!): Session!
  deleteColumn(id: String!, session: String!): Session
  deleteSession(name: String!): Boolean
  toggleSession(name: String!, status: SessionStatus!): Session!
  updateColumn(id: String!, isFocus: Boolean, label: String, position: Float, session: String!): Session!
  updateSession(isModerated: Boolean!, isOpen: Boolean!, name: String!): Session!
}

type Query {
  getSession(name: String!): Session
  listSessions: [Session!]!
}

"""The Session model"""
type Session {
  columns: [SessionColumn!]!
  end: DateTime
  id: ID!
  isModerated: Boolean!
  isOpen: Boolean!
  name: String!
  start: DateTime
  status: SessionStatus!
}

"""The Session column model"""
type SessionColumn {
  id: ID!
  isFocus: Boolean!
  label: String!
  position: Float!
}

enum SessionStatus {
  break
  done
  pomodoro
  unbegun
}
