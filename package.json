{
  "private": true,
  "workspaces": [
    "common",
    "backend",
    "frontend"
  ],
  "scripts": {
    "common": "yarn workspace common",
    "backend": "yarn workspace backend",
    "frontend": "yarn workspace frontend",
    "serve": "concurrently \"yarn frontend start\" \"wait-on http://localhost:3000 && yarn backend dev\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "wait-on": "^5.2.0"
  }
}